# RFC 018 live-fixture policy witness

This intentionally public file authorizes the hand-authored candidate text in
`queries.yml` for live M0/M1 evaluation. It is a policy/provenance witness,
not the source content represented by any individual replayed candidate.
